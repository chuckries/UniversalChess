<Page
    x:Class="SampleApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleApp"
    xmlns:chess="using:UniversalChess.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:OrientationRadioConverter x:Key="OrientationRadioConverter"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center">
            <chess:ChessBoard x:Name="TheChessBoard" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="Flip Board" Click="FlipBoardButton_Click"/>
                <ToggleSwitch Header="Orientation" OffContent="White" OnContent="Black" IsOn="{x:Bind TheChessBoard.Orientation, Mode=TwoWay, Converter={StaticResource OrientationRadioConverter}}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
